services:
  - type: web
    name: authchain-backend
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libzbar0
      pip install -r requirements.txt
    startCommand: gunicorn server:app
